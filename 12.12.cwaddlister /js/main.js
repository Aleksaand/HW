// `use strick`;
// сonst randomInt = (min, max) => {
//     let rand = min + Math.random() * (max + 1 - min);
//     return Math.floor(rand);
// }
for (i = 0; i < 144; i++) {
  mainEl.innerHTML = '<div> i </div>';
}







// const inputEl = document.querySelector('.sq');

// inputEl.addEventListener('click', () => {
//     console.log('click');
// });

// console.log(inputEl);

// inputEl.style.background = 'red';